# Mumbai Traffic Network Planning — Baseline dependencies
# Python 3.11+ (developed on 3.12)

# --- Network construction & graph operations ---
osmnx>=2.0
networkx>=3.2

# --- Geospatial ---
geopandas>=1.0
shapely>=2.0

# --- Numeric / scientific ---
numpy>=1.26
scipy>=1.11
pandas>=2.1

# --- Visualization ---
matplotlib>=3.8
folium>=0.16
mapclassify>=2.6   # required by geopandas .explore() interactive maps

# --- API interaction (Phase 0.7+ — TomTom Flow/Routing/Matrix; primary source) ---
# Plain REST with the key as a query param — no vendor client library needed.
requests>=2.31
# googlemaps>=4.10   # only if the Google fallback (plan §3.2) is ever activated

# --- Notebook environment ---
jupyterlab>=4.0

# --- Traffic assignment (evaluate for Phase 3; optional) ---
# aequilibrae>=1.0   # uncomment once network model is validated
