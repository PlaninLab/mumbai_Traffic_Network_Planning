# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# --- Virtual environment ---
.venv/
venv/
env/

# --- Secrets / API keys ---
.env
*.key
secrets.json
config.local.*

# --- Large / raw data (keep dir structure via .gitkeep, ignore contents) ---
data/raw/osm/*
data/raw/google_api/*
data/raw/tomtom/*
!data/**/.gitkeep
# ...but DO version the small collected flow-summary CSVs (the observed dataset).
!data/raw/tomtom/collected/
data/raw/tomtom/collected/*
!data/raw/tomtom/collected/*.csv

# --- Generated network binaries (reproducible via src/network/build_network.py) ---
*.graphml
*.gpkg
# ...except the corridor graph. src/data/collect_flow.py loads it on EVERY reading to
# place the WEH sample points, so the collector needs it at runtime on the server. It is
# also the reason `idx` means the same physical location in every reading ever taken —
# a rebuilt graph would shift the points and break the per-point congestion profile.
!data/raw/osm/corridor.graphml

# --- SQLite reading store (binary; reproducible via `python -m src.data.store --backfill`) ---
*.db
*.sqlite
*.sqlite3

# --- osmnx / tooling caches ---
cache/
.claude/settings.local.json

# --- OS ---
.DS_Store
Thumbs.db

# --- Jupyter ---
*.ipynb_checkpoints
